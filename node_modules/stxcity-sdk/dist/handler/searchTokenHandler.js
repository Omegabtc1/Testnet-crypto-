"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.searchTokenHandler = void 0;
const config_1 = require("../config");
const searchTokenHandler = (params) => __awaiter(void 0, void 0, void 0, function* () {
    const { keyword, tokenContract } = params || {};
    let url = `${config_1.configs.STXCITY_API_HOST}/api/searchTokens`;
    if (tokenContract) {
        url = `${config_1.configs.STXCITY_API_HOST}/api/searchTokens?token_contract=${tokenContract}`;
    }
    else if (keyword) {
        url = `${config_1.configs.STXCITY_API_HOST}/api/searchTokens?keyword=${keyword}`;
    }
    try {
        const response = yield fetch(url);
        if (!response.ok) {
            throw new Error(`HTTP error! status: ${response.status}`);
        }
        const data = yield response.json();
        if (data) {
            // Handle success
            const dataSearch = data;
            return dataSearch;
        }
        else {
            console.error("Unexpected data structure returned by API:", data);
            throw new Error("Unexpected data structure returned by API");
        }
    }
    catch (error) {
        console.error(`Error fetching data from ${url}:`, error);
        throw error;
    }
});
exports.searchTokenHandler = searchTokenHandler;
//# sourceMappingURL=searchTokenHandler.js.map